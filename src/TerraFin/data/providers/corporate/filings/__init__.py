# Filings package — sub-packages live here (e.g. `sec_edgar`).
#
# Use `TerraFin.data.providers.corporate.filings.sec_edgar` for the SEC EDGAR
# API (`get_sec_data`, `get_sec_toc`, `parse_sec_filing`, `build_toc`).
